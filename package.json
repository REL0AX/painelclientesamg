{
  "name": "painel-clientes-amg",
  "private": true,
  "scripts": {
    "serve": "http-server public -a 127.0.0.1 -p 4173 -c-1",
    "firebase:login": "firebase login",
    "firebase:emulators": "firebase emulators:start --only hosting,firestore,auth",
    "firebase:deploy:auth": "firebase deploy --only auth",
    "firebase:deploy:hosting": "firebase deploy --only hosting",
    "firebase:deploy:rules": "firebase deploy --only firestore:rules",
    "firebase:deploy": "firebase deploy --only hosting,firestore:rules,auth"
  },
  "devDependencies": {
    "firebase-tools": "^15.12.0",
    "http-server": "^14.1.1"
  }
}
